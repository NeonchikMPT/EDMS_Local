# 🚀 EDMS - Система управления электронными документами

**EDMS** (Electronic Document Management System) — это Django-приложение для управления электронными документами. Оно позволяет:

- 📤 Загружать и хранить документы
- 🖼️ Управлять аватарками пользователей
- 👤 Настраивать профили и роли через админ-панель

Медиа-файлы (аватарки и документы) хранятся локально в папках `media/avatars/` и `media/docs/`. Проект предназначен для **локального запуска** и идеально подходит для тестирования или обучения.

---

## 📋 Содержание

- [🔍 О проекте](#-о-проекте)
- [🛠 Требования](#-требования)
- [⚙️ Установка](#-установка)
- [📂 Настройка медиа-файлов](#-настройка-медиа-файлов)
- [🚀 Запуск](#-запуск)
- [🎮 Использование](#-использование)
- [🛡️ Устранение неполадок](#-устранение-неполадок)
- [📁 Структура проекта](#-структура-проекта)
- [📜 Лицензия](#-лицензия)
- [📬 Контакты](#-контакты)

---

## 🔍 О проекте

EDMS — это система для управления документами и пользовательскими профилями. Она построена на **Django 5.2.1** и использует **PostgreSQL** для хранения данных. Все файлы (аватарки, документы) сохраняются локально, что упрощает настройку для разработки.

**Ключевые особенности**:
- Простая загрузка аватарок (JPG, PNG) и документов (PDF, DOCX, TXT)
- Удобная админ-панель для управления
- Локальное хранение медиа-файлов
- Поддержка кастомной модели пользователя

![Скриншот интерфейса](https://via.placeholder.com/600x300.png?text=EDMS+Interface)  
*Скриншот главной страницы (замените на реальный, если есть)*

---

## 🛠 Требования

Для запуска EDMS локально вам понадобятся:

| Требование       | Версия/Описание               |
|------------------|-------------------------------|
| 🐍 **Python**    | 3.11.9                       |
| 🗄 **PostgreSQL** | Последняя стабильная версия   |
| 📦 **Git**       | Для клонирования репозитория  |
| 🔧 **pip**       | Для установки зависимостей    |
| 🌐 **venv**      | Для создания виртуальной среды |

---

## ⚙️ Установка

Следуйте этим шагам, чтобы настроить и запустить EDMS на вашем компьютере.

### 1. Клонируйте репозиторий

```bash
git clone https://github.com/NeonchikMPT/EDMS_Local.git
cd EDMS_Local
```

### 2. Создайте виртуальную среду

Изолируйте зависимости с помощью виртуальной среды.

На **Windows**:

```bash
python -m venv .venv
.venv\Scripts\activate
```

На **macOS/Linux**:

```bash
python3 -m venv .venv
source .venv/bin/activate
```

### 3. Установите зависимости

Установите пакеты из `requirements.txt`:

```bash
pip install -r requirements.txt
```

Ключевые зависимости:
- `django==5.2.1`
- `psycopg2-binary==2.9.10`
- `pillow==11.2.1` (для обработки изображений)
- `python-decouple==3.8`

### 4. Настройте PostgreSQL

1. Убедитесь, что PostgreSQL установлен и запущен.
2. Создайте базу данных:

   ```bash
   psql -U postgres
   ```

   В PostgreSQL выполните:

   ```sql
   CREATE DATABASE edms_db;
   \q
   ```

### 5. Настройте файл `.env`

Создайте файл `.env` в корне проекта со следующим содержимым:

```env
DB_NAME=edms_db
DB_USER=postgres
DB_PASSWORD=your_postgres_password
DB_HOST=localhost
DB_PORT=5432
SECRET_KEY=your_secure_secret_key
DJANGO_DEBUG=True
```

- Замените `your_postgres_password` на ваш пароль PostgreSQL.
- Сгенерируйте `SECRET_KEY`:

  ```bash
  python -c "import secrets; print(secrets.token_urlsafe(50))"
  ```

---

## 📂 Настройка медиа-файлов

EDMS хранит медиа-файлы локально:
- **Аватарки** → `media/avatars/`
- **Документы** → `media/docs/`

1. Создайте папки:

   ```bash
   mkdir -p media/avatars media/docs
   ```

2. Установите права на запись (для macOS/Linux):

   ```bash
   chmod -R 755 media
   ```

На Windows права обычно не требуют настройки.

3. Убедитесь, что в `settings.py` настроено:

```python
MEDIA_URL = '/media/'
MEDIA_ROOT = BASE_DIR / 'media'
```

---

## 🚀 Запуск

1. Примените миграции для базы данных:

   ```bash
   python manage.py makemigrations
   python manage.py migrate
   ```

2. Создайте суперпользователя для админ-панели:

   ```bash
   python manage.py createsuperuser
   ```

   Введите:
   - **Username**: например, `admin`
   - **Email**: например, `admin@example.com`
   - **Password**: например, `Admin123!`

3. Запустите сервер:

   ```bash
   python manage.py runserver
   ```

4. Откройте в браузере:
   - Главная страница: `http://localhost:8000`
   - Админ-панель: `http://localhost:8000/admin`

---

## 🎮 Использование

- **Загрузка аватарок**:
  1. Перейдите на `http://localhost:8000/users/profile/`.
  2. Выберите изображение (JPG, PNG).
  3. Нажмите "Сохранить". Аватарка появится в `media/avatars/`.

- **Загрузка документов**:
  1. Перейдите на страницу документов (например, `/docs/`).
  2. Выберите файл (PDF, DOCX, TXT).
  3. Нажмите "Загрузить". Файл сохранится в `media/docs/`.

- **Админ-панель**:
  1. Войдите с данными суперпользователя.
  2. Управляйте пользователями, документами и настройками.

---

## 🛡️ Устранение неполадок

| Проблема                          | Решение                                                                 |
|-----------------------------------|------------------------------------------------------------------------|
| **Ошибка базы данных**            | Проверьте `DB_*` в `.env` и убедитесь, что PostgreSQL запущен.         |
| **Медиа-файлы не загружаются**    | Убедитесь, что папки `media/avatars/` и `media/docs/` существуют. Проверьте `enctype="multipart/form-data"` в формах. |
| **Изображения не отображаются**   | Убедитесь, что `pillow` установлен (`pip install pillow`). Проверьте `MEDIA_URL` и `MEDIA_ROOT`. |
| **Ошибки миграций**               | Удалите старые миграции: `find . -path "*/migrations/*.py" -not -name "__init__.py" -delete`, затем `makemigrations` и `migrate`. |

---

## 📁 Структура проекта

```plaintext
EDMS_Local/
├── media/              # Локальное хранилище медиа-файлов
│   ├── avatars/        # Аватарки пользователей
│   └── docs/           # Документы
├── users/              # Приложение для пользователей
├── docs/               # Приложение для документов
├── templates/          # HTML-шаблоны
├── manage.py           # Управление проектом
├── requirements.txt    # Зависимости
└── README.md           # Этот файл
```

---

## 📜 Лицензия

Проект распространяется под лицензией **MIT**. Подробности в файле `LICENSE`.

---

## 📬 Контакты

Если у вас есть вопросы или предложения, свяжитесь с автором:  
👤 **NeonchikMPT**  
🔗 [GitHub](https://github.com/NeonchikMPT)

---

*Создано с ❤️ для упрощения работы с документами!*
